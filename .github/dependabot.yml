version: 2
updates:
  # Keeps this repo's action pins current. Consumers track the verify.yml ref
  # via their own Dependabot (see README).
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 5
    cooldown:
      default-days: 7
